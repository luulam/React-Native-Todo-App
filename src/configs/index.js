import { constants, colors, styleApp } from './theme';
import realm from './realm';

const configs = {
    client_id: '',
    client_secret: '',
    time_show_notify: 2000
};

export {
    configs, constants, colors, styleApp, realm
};
